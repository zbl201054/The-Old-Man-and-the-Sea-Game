# 《老人与海》互动叙事游戏 —— 存档与设置系统功能文档（含历史记录展示）


## 一、概述

### 1.1 功能目标
为游戏提供完整、健壮的**存档管理**与**玩家设置**能力，包括：
- 自动/手动存档，记录完整游戏状态（进度、数值、成就、解锁结局、**完整的历史节点序列**等）
- **导入/导出存档**（JSON 格式），支持玩家跨设备转移进度
- **旧版本存档兼容与升级**，确保游戏更新后玩家进度不丢失
- 提供统一的**设置面板**，集中管理存档操作、游戏偏好、显示信息等
- **显示每次选择的节点及结局**，让玩家清晰回顾自己的决策路径
- 显示游戏版本、著作权信息，符合开源合规要求

### 1.2 适用范围
本功能独立于核心剧情引擎，以“插件”形式集成，适用于游戏的所有版本（标准版、扩展版、完整版）。所有功能均采用 **ES5 语法**，兼容 Windows 7（IE11/Chrome49/Firefox52）。


## 二、数据模型（存档格式）

### 2.1 存档结构定义（JSON Schema）

```json
{
  "version": "3.0.0",               // 存档版本号（语义化）
  "gameVersion": "3.0.0",           // 游戏版本号
  "timestamp": 1700000000000,       // 存档时间戳（毫秒）
  "gameState": {
    "willpower": 5,
    "stamina": 3,
    "insight": 2,
    "flags": ["hasBoy", "missBoy"],
    "currentNode": "N18",
    "history": ["N1","N2","N3","N4","N5","N6","N7","N8","N9","N10","N11","N12","N13","N14","N15","N16","N17","N18"],
    "endingsUnlocked": ["E1","E3"],
    "achievements": ["A01","A04","A12"],
    "achievementProgress": {
      "totalPlaythroughs": 2,
      "totalSaves": 5,
      "totalLoads": 3,
      "sharkFightCount": 4,
      "sharkFleeCount": 1,
      "nodesSurvivedLowStamina": 3,
      "rejectSurrenderCount": 6
    },
    "playCount": 2
  },
  "settings": {
    "memeMode": true,
    "autoSave": true,
    "soundEnabled": false,
    "textSpeed": 1.0
  },
  "metadata": {
    "playerName": "圣地亚哥",
    "totalPlayTime": 3600,          // 秒
    "lastPlayDate": "2026-08-18T14:30:00Z"
  }
}
```

### 2.2 关键字段说明
| 字段 | 类型 | 说明 |
|------|------|------|
| `history` | string[] | **完整的选择节点序列**，按时间顺序存储所有访问过的节点ID（包括结局节点E1~E12）。用于在“历史记录”和“存档详情”中展示玩家的决策路径。 |
| `endingsUnlocked` | string[] | 已解锁的结局ID列表（全局累积） |
| `currentNode` | string | 当前所在节点ID（可能是剧情节点或结局节点） |
| `version` / `gameVersion` | string | 存档格式版本和游戏版本，用于升级兼容 |

### 2.3 节点显示名称映射
为在UI中友好展示节点，需为每个节点定义显示名称。在 `gameData.nodes` 和 `gameData.endings` 中已包含 `scene` 或 `title` 字段，显示时可直接使用。若缺少，可回退为节点ID。


## 三、核心功能设计

### 3.1 历史记录（History）—— 存储与展示

#### 3.1.1 存储方式
- 内存中：`gameState.history` 数组，在每次 `renderNode()`（包括剧情节点和结局节点）时追加节点ID。
- 存档时直接序列化整个数组，读档时完整恢复。

#### 3.1.2 历史记录展示（UI）
在设置面板中提供 **“📊 历史记录”** 标签页，以**时间线**形式展示当前游戏已走过的所有节点：
- 每个节点显示其场景标题（或简写），并用图标区分类型：剧情节点用 📖，结局节点用 🏆。
- 当前所在节点（最后一个）高亮显示。
- 显示总节点数和已完成百分比（相对总节点数）。
- 提供 **“导出历史为文本”** 按钮，生成纯文本路径，方便玩家分享或复盘。

#### 3.1.3 存档详情中的历史显示
在 **“📂 存档管理”** 标签页中，每个存档槽位可**展开查看详情**（或点击“查看路径”按钮），显示该存档的完整历史节点序列（与上述时间线样式一致），并特别标注最终结局（若当前节点为结局，则突出显示结局标题）。

#### 3.1.4 历史记录与存档的关系
- 存档中存储的是**完整的历史记录**，因此导出的存档包含完整的路径信息。
- 导入存档时，历史记录随进度一同恢复，并可在新游戏中继续追加。

### 3.2 自动存档与手动存档
（同原设计，略）  
自动存档在每次选择后触发，手动存档提供多个槽位，支持覆盖、删除、重命名。

### 3.3 导入 / 导出存档
（同原设计，略）  
导入/导出的 JSON 文件包含完整的历史记录。

### 3.4 旧版本存档兼容与升级
（同原设计，略）  
升级时确保 `history` 字段存在（若旧版无 `history`，则初始化为空数组或根据现有逻辑重建）。

### 3.5 存档版本显示与管理
（同原设计，略）  
在存档列表中显示每个存档的版本号，并提供升级功能。

### 3.6 设置页面设计
（同原设计，但强化历史记录展示）  
设置面板包含四个标签页：
1. **📂 存档管理**：列表展示所有槽位，每个槽位可展开查看详细路径。
2. **🎮 游戏设置**：玩家偏好。
3. **📊 历史记录**：当前游戏的时间线路径，可导出。
4. **ℹ️ 关于**：版本与著作权信息。


## 四、UI 设计草图（含历史记录展示）

### 4.1 主界面右上角
```
[⚙️ 设置]   [💾 快速存档 (Ctrl+S)]
```

### 4.2 设置模态框

#### 4.2.1 存档管理标签页（展开的存档详情）
```
┌─────────────────────────────────────────────────────┐
│  ⚙️ 设置                              [✕ 关闭]    │
├─────────────────────────────────────────────────────┤
│  [📂 存档管理]  [🎮 游戏设置]  [📊 历史]  [ℹ️ 关于] │
├─────────────────────────────────────────────────────┤
│  当前游戏版本: v3.0.0                               │
│                                                     │
│  ─── 存档槽位 ───                                  │
│  ● 自动存档  2026-08-18 14:30  精神5 体力3  E1/E3  │
│    [读取] [覆盖] [导出] [📜 查看路径]              │
│    └─ 点击“查看路径”后展开：                       │
│       📖 海岸·茅棚 → 📖 茅棚门口 → ... → 🏆 硬汉的荣耀 │
│       (共17个节点，最终结局：硬汉的荣耀)            │
│                                                     │
│  ○ 快速存档  2026-08-18 14:25  精神4 体力2  E1    │
│    [读取] [覆盖] [导出] [📜 查看路径]              │
│                                                     │
│  ○ 手动1     2026-08-17 20:10  精神6 体力4  E1/E2 │
│    [读取] [覆盖] [导出] [删除] [📜 查看路径]      │
│                                                     │
│  ○ 手动2     (空)                                  │
│    [新建存档]                                      │
│                                                     │
│  [导入存档]  [导出当前存档]  [导出所有存档]         │
│  ☑ 自动存档 (每次选择后自动保存)                    │
│  📦 存档版本: 全部为 v3.0.0 (最新)                  │
│  [一键升级所有旧存档]                               │
└─────────────────────────────────────────────────────┘
```

#### 4.2.2 历史记录标签页
```
┌─────────────────────────────────────────────────────┐
│  📊 历史记录                                       │
├─────────────────────────────────────────────────────┤
│  当前游戏路径（共 17 个节点，完成度 56%）           │
│                                                     │
│  ⏳ 节点序列（按时间顺序）：                        │
│  1. 📖 N1 海岸·茅棚                                │
│  2. 📖 N2 茅棚门口                                 │
│  3. 📖 N3 黎明码头                                 │
│  4. 📖 N4 远海·正午                                │
│  5. 📖 N5 烈日下                                   │
│  6. 📖 N6 海面                                     │
│  7. 📖 N7 傍晚·大鱼咬钩                            │
│  8. 📖 N9 僵持·对话鱼                              │
│  9. 📖 N10 第一夜僵持                              │
│  10. 📖 N11 深夜·观星                              │
│  11. 📖 N12 星海指引                               │
│  12. 📖 N13 第二天黎明                             │
│  13. 📖 N14 生鱼充饥                               │
│  14. 📖 N15 大鱼跃水                               │
│  15. 📖 N16 最后一击                               │
│  16. 📖 N16.5 猎杀成功                             │
│  17. 📖 N17 返航·孤独                              │
│  18. 📖 N18 灰鲭鲨初袭 ← 当前位置                 │
│                                                     │
│  后续节点预测（基于常见选择）：                      │
│  → N19 → N20 → ... → E1/E2/E3...                  │
│                                                     │
│  [📤 导出历史为文本]  [🗑️ 清空历史（谨慎）]        │
└─────────────────────────────────────────────────────┘
```

#### 4.2.3 关于标签页
```
┌─────────────────────────────────────────────────────┐
│  ℹ️ 关于                                            │
├─────────────────────────────────────────────────────┤
│        ⚓ 老人与海 · 互动叙事游戏                    │
│  版本: 3.0.0 (Build 2026.08.18)                    │
│  基于海明威中篇小说《老人与海》改编                  │
│  高中语文选择性必修上册 互动学习辅助作品             │
│  --- 开发团队 ---                                   │
│  策划/剧本: [姓名]                                  │
│  程序/UI: [姓名]                                    │
│  © 2026 [工作室/个人] 保留所有权利                  │
│  本游戏采用 MIT 许可证开源                          │
│  源码地址: https://github.com/xxx/oldman-game      │
│  特别致敬: 欧内斯特·海明威                          │
└─────────────────────────────────────────────────────┘
```


## 五、技术实现指南（关键代码片段）

### 5.1 历史记录追加（在 renderNode 中）
```javascript
function renderNode(nodeId) {
    // ... 原有逻辑 ...
    // 追加到历史记录（避免重复追加同一个节点，但允许结局重复？通常不会）
    if (gameState.history[gameState.history.length - 1] !== nodeId) {
        gameState.history.push(nodeId);
    }
    // ... 其他逻辑 ...
}
```

### 5.2 生成历史显示的友好名称
```javascript
function getNodeDisplayName(nodeId) {
    var node = gameData.nodes[nodeId];
    if (node) return node.scene || nodeId;
    var ending = gameData.endings[nodeId];
    if (ending) return '🏆 ' + ending.title;
    return nodeId;
}
```

### 5.3 在存档管理列表中显示路径（展开详情）
```javascript
function renderSaveSlotDetail(slotName, data) {
    var history = data.gameState.history || [];
    var html = '<div style="margin-top:8px;padding:8px 12px;background:#0f232b;border-radius:6px;">';
    html += '<div style="font-size:13px;color:#8aa9b3;">路径（共' + history.length + '个节点）：</div>';
    var items = [];
    for (var i = 0; i < history.length; i++) {
        var id = history[i];
        var name = getNodeDisplayName(id);
        var icon = id.indexOf('E') === 0 ? '🏆' : '📖';
        items.push(icon + ' ' + name);
    }
    html += '<div style="font-size:13px;line-height:1.6;max-height:120px;overflow-y:auto;">' + items.join(' → ') + '</div>';
    // 显示最终结局
    var last = history[history.length - 1];
    if (last && last.indexOf('E') === 0) {
        html += '<div style="margin-top:6px;color:#c9a86c;">最终结局：' + getNodeDisplayName(last) + '</div>';
    }
    html += '</div>';
    return html;
}
```

### 5.4 历史记录导出为文本
```javascript
function exportHistoryAsText() {
    var history = gameState.history || [];
    var lines = ['《老人与海》游戏路径记录', '生成时间：' + new Date().toLocaleString(), ''];
    for (var i = 0; i < history.length; i++) {
        var id = history[i];
        var name = getNodeDisplayName(id);
        lines.push((i+1) + '. ' + name + ' (' + id + ')');
    }
    var text = lines.join('\n');
    var blob = new Blob([text], { type: 'text/plain' });
    var url = URL.createObjectURL(blob);
    var a = document.createElement('a');
    a.href = url;
    a.download = 'history_' + new Date().toISOString().slice(0,10) + '.txt';
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
}
```


## 六、开发测试要点（新增）

| 测试项 | 步骤 | 预期结果 |
|--------|------|----------|
| 历史记录记录 | 进行多个选择，打开历史记录标签页 | 显示完整、有序的节点列表 |
| 存档详情显示路径 | 保存一个存档，在存档管理列表点击“查看路径” | 展开显示该存档的完整路径和最终结局 |
| 导出历史文本 | 点击“导出历史为文本” | 下载包含节点路径的文本文件 |
| 导入存档后历史恢复 | 导出一个存档，再导入到新槽位，加载后查看历史 | 历史记录与导出时一致 |


## 七、关于与版本显示（固定内容）

在“关于”页面中显示：
- **游戏名称**：《老人与海》互动叙事游戏
- **当前版本**：从常量 `GAME_VERSION` 读取（如 `v3.0.0`）
- **著作权**：© 2026 曾泊霖。基于海明威原著，仅供学习交流使用。
- **开源许可**：MIT License
- **第三方资源**：字体、图标等，标明出处。
- **工具调用**：Deepseek API 用于生成游戏内容。


## 八、总结

本功能文档在原有设计基础上，**强化了历史记录的展示与存档详情**，使玩家能够清晰回顾每次选择的节点和最终结局，提升了游戏的复盘体验和叙事沉浸感。所有功能均保持与主游戏引擎的解耦，易于集成和测试。

---

*文档版本：1.1 | 适用游戏版本：3.0.0 | 最后更新：2026-08-18*