## 程序流程梳理

### 1. 整体架构
游戏采用**状态驱动**的交互叙事引擎，核心数据结构为 `gameState` 与预定义的 `gameData`（节点/结局）。主要模块包括：

- **初始化模块**：恢复存档（localStorage / 文件系统），初始化设置，启动首节点渲染。
- **渲染模块**：`renderNode()` 根据节点ID显示场景文本、选项按钮或过渡动画；处理隐藏结局触发。
- **选择处理模块**：`makeChoice()` 应用属性变化、标记，检查体力归零逻辑，跳转下一节点。
- **结局模块**：`renderEnding()` 展示结局内容，记录解锁，触发成就检测。
- **成就系统**：`checkAchievements()` 定期检测18项成就条件，解锁时弹出Toast通知并更新UI。
- **存档系统**：支持多槽位（自动、快速、手动1/2）存储，可导入/导出JSON，并支持文件系统（`save.json`）。
- **设置面板**：调节梗模式、自动存档、音效（占位）、文本速度。

### 2. 核心生命周期流程（简化）

```
用户打开页面
    ↓
initGame()
    ├── 恢复 localStorage 或 save.json 中的游戏状态
    ├── 若存在存档，还原 gameState
    └── 调用 renderNode(当前节点)
            ↓
renderNode(nodeId)
    ├── 取消待执行的过渡定时器
    ├── 重置 _hiddenEnding = null  （‼️ 需修复）
    ├── 检查并设置隐藏结局（checkHiddenEndings）
    ├── 更新场景标题/章节/故事文本（支持梗模式彩蛋）
    ├── 若节点为 transition 类型：
    │   ├── 显示过渡提示
    │   ├── 若存在 _hiddenEnding，跳转隐藏结局，否则跳转 node.next
    │   └── 延迟 2.5s 后调用 renderNode(目标节点)
    ├── 否则（普通节点）：
    │   ├── 若为 N1 且已解锁 ≥2 个结局，动态追加 E11 选项
    │   ├── 根据选项数量设置 CSS 布局类
    │   └── 为每个选项绑定点击事件 → makeChoice(index)
    ├── 保存当前进度（saveGameSilent）
    └── 更新 UI 状态条和成就面板
            ↓
makeChoice(index)
    ├── 取消待执行的过渡定时器
    ├── 获取选中选项
    ├── 追踪彩蛋计数（放弃次数、连续独自承担等）
    ├── 记录成就相关进度（鲨鱼战斗/逃避次数等）
    ├── 应用属性变化（will/stamina/insight）
    ├── 限制属性在 [0, max] 范围内
    ├── 添加选项标记（flag）
    ├── 若体力 ≤ 0 且下一节点非结局：
    │   ├── 沿过渡链查找最多3层，检查后继节点是否含有结局选项
    │   └── 若无可达结局选项，强制跳转 E3（海之归途）
    ├── 调用 renderNode(choice.next)
    └── 检查成就（checkAchievements）
            ↓
renderEnding(endingId)
    ├── 记录结局解锁（若未解锁）
    ├── 设置 _e2NoBoy 标记（用于 A14 孤高之王）
    ├── 显示结局标题、等级、文本（支持梗模式彩蛋）
    ├── 提供“再次出海”和（E11专属）“继续航行”按钮
    ├── 显示一周目梗总结（可选）
    ├── 更新图鉴、存档、UI状态
    └── 检查成就（checkAchievements）
```

### 3. 隐藏结局触发逻辑（在 `checkHiddenEndings` 中）

| 隐藏结局 | 触发节点 | 条件 |
|---------|---------|------|
| E6（狮子的传人） | N16_5 | 拥有 `hasBoy` 标记，精神 ≥5，体力 ≤1 |
| E9（渔夫的遗产） | N13   | 感悟 ≥3，精神 ≥5 |
| E10（东方之星） | N12   | 拥有 `starGuide` 标记，感悟 ≥4 |

### 4. 成就系统检测时机
- 每次选择后 (`makeChoice`)
- 每次结局渲染后 (`renderEnding`)
- 存档/读档时
- 其他关键操作（重启等）后自动触发

---

## 错误列表

### 严重错误
1. **`_hiddenEnding` 残留问题**  
   - **位置**：`renderNode` 函数开头未重置 `gameState._hiddenEnding = null;`  
   - **影响**：若前一个渲染节点曾设置隐藏结局（但未触发），在当前节点不满足隐藏条件时，`_hiddenEnding` 仍保留旧值，可能导致后续过渡节点错误跳转到不相关的隐藏结局。  
   - **修复**：在 `renderNode` 第一行添加 `gameState._hiddenEnding = null;`

### 潜在逻辑缺陷（非致命）
2. **体力归零强制 E3 的搜索深度过浅**  
   - 当前只沿过渡链查找 **3层**，若游戏未来增加更长的过渡链，可能漏判可结局选项。  
   - 建议改为循环查找直到找到非过渡节点。

3. **`restartGame` 未重置部分彩蛋计数**  
   - 重置了 `_abandonCount`、`_lonerConsecutive` 等，但未重置 `achievementProgress._e2NoBoy`（但此为全局成就标记，保留是合理的）。  
   - 不影响功能，但需理解设计意图。

4. **梗模式中的 `alert` 打断流程**  
   - 在 `_abandonCount >= 2` 时弹出 `alert`，虽不影响核心逻辑，但体验欠佳。

### 兼容性问题（非错误）
5. **File System Access API** 仅在支持该API的浏览器（Chromium）中工作，且需安全上下文（HTTPS）。在 IE11 或旧版浏览器中无效，但已有 `localStorage` 作为回退。

### 样式/UI 小瑕疵
6. **`#achievement-grid` 初始状态不显示**，但 `achievement-title` 点击后正常切换，无功能影响。

---

## 流程图（文字描述）

```
[启动] 
   │
   ▼
[读取本地存档] ──失败──▶ [使用默认状态]
   │
   ▼
[渲染首节点 (N1)]
   │
   ├─▶ [显示场景文本 & 选项]
   │       │
   │       ▼
   │   [用户点击选项] ──▶ [makeChoice]
   │       │
   │       ├─▶ [应用属性变化、标记]
   │       ├─▶ [体力≤0 且后继无可达结局？] ──是──▶ [强制跳转 E3]
   │       │                    │
   │       │                    └─▶ [renderNode(目标节点)]
   │       ▼
   └─▶ [若节点为 transition]
           │
           ├─▶ [显示过渡文字，延迟2.5s]
           ├─▶ [若 _hiddenEnding 存在] ──▶ [跳转隐藏结局]
           └─▶ [否则跳转 node.next]
```

**结局分支**：任何路径最终都会进入 `renderEnding`，展示结局并解锁成就。

---

### 建议修复代码
在 `renderNode` 函数开头插入一行：
```javascript
gameState._hiddenEnding = null;
```

确保每次重新渲染时清除之前可能残留的隐藏结局标记。